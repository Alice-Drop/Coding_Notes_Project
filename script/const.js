let PROJECT_NAME = "AliceDrop知识库共享"


// 顶部标题栏的高度(按border-box计)
const NAV_HEIGHT = 52;

const NAV_MIN_WIDTH = 980;


/* ======== reader ========= */

// reader的toc控制
const TOC_STICKED_Y = 70;

const TOC_SELECTED_TOP_AGNORE = 100;

// reader跳转到某个标题，需要往上滚一点来消除标题栏影响
const TOC_GOTO_MARGIN_TOP = -(NAV_HEIGHT + 20);

const DOC_CONTENT_STD_WIDTH = 300;

const TOC_SELECTED_RANGE_0 = 50;
const TOC_SELECTED_RANGE_1 = 100;